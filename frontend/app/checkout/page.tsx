'use client'

export default function CheckoutPage() {
  return (
    <div
      style={{
        minHeight: '100vh',
        backgroundColor: 'var(--bg)',
        color: 'var(--fg)',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontFamily: "'Courier New', Courier, monospace",
      }}
    >
      <p style={{ fontSize: '14px', letterSpacing: '0.2em', textTransform: 'uppercase' }}>
        CHECKOUT — COMING SOON
      </p>
    </div>
  )
}
